document.onkeydown=function(e)
{
    switch(e.keycode)
    {
        case 32:
        
        //フィールドサイズ
        const FIELD_W=200;
        const FIELD_H=100;

        //スクリーンサイズ
        const SCREEN_W=FIELD_W*2;
        const SCREEN_H=FIELD_H*2;

        //キャンバスサイズ
        const CANVAS_W=SCREEN_W;
        const CANVAS_H=SCREEN_H;

        //


    }
}